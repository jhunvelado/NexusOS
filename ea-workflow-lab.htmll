<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>EA Workflow Lab — Train Like You Work | Powered by NexusOS</title>
<meta name="description" content="The hands-on training simulator for Executive Assistants. Practice real EA workflows on real tools — before your first real client.">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/tabler-icons.min.css">
<style>
*{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
body{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;background:#0a0a12;color:#f0f0f0;line-height:1.6}

/* COLORS */
:root{
  --navy:#0a0a12;--navy2:#0f0f1a;--navy3:#13131f;--navy4:#1a1a2a;
  --purple:#6C63FF;--purple2:#534AB7;--purple-soft:#8B84FF;
  --gold:#f0b429;--gold2:#d9a320;
  --green:#1D9E75;--red:#E24B4A;--blue:#378ADD;
  --text:#f0f0f0;--muted:#888;--muted2:#555;--line:#1e1e2e;
  --radius:12px;
}

/* NAV */
nav{position:sticky;top:0;z-index:100;background:rgba(10,10,18,0.95);backdrop-filter:blur(12px);border-bottom:1px solid var(--line);padding:14px 24px;display:flex;align-items:center;justify-content:space-between}
.nav-logo{color:var(--gold);font-weight:700;font-size:17px;text-decoration:none;display:flex;align-items:center;gap:8px}
.nav-logo span{color:var(--purple-soft);font-size:13px;font-weight:500;padding:3px 10px;background:rgba(108,99,255,0.15);border-radius:20px;border:1px solid rgba(108,99,255,0.3)}
.nav-right{display:flex;align-items:center;gap:16px}
.nav-right a{color:var(--muted);font-size:13px;text-decoration:none}
.nav-right a:hover{color:var(--text)}
.btn-nav{background:var(--gold);color:#1a1400;padding:8px 18px;border-radius:8px;font-size:13px;font-weight:700;text-decoration:none}

/* HERO */
.hero{padding:72px 24px 56px;text-align:center;position:relative;overflow:hidden}
.hero::before{content:'';position:absolute;top:-100px;left:50%;transform:translateX(-50%);width:700px;height:400px;background:radial-gradient(ellipse,rgba(108,99,255,0.18),transparent 70%);pointer-events:none}
.hero-badge{display:inline-flex;align-items:center;gap:7px;background:var(--navy3);border:1px solid rgba(108,99,255,0.3);color:var(--purple-soft);font-size:11px;font-weight:600;padding:5px 14px;border-radius:20px;margin-bottom:20px;letter-spacing:0.05em}
.hero h1{font-size:clamp(32px,5vw,54px);font-weight:700;line-height:1.1;margin-bottom:16px;letter-spacing:-0.02em}
.hero h1 em{font-style:normal;color:var(--gold)}
.hero-sub{font-size:clamp(14px,2vw,17px);color:var(--muted);max-width:560px;margin:0 auto 32px;line-height:1.75}
.hero-ctas{display:flex;gap:12px;justify-content:center;flex-wrap:wrap;margin-bottom:20px}
.btn-lg{padding:13px 28px;border-radius:10px;font-size:14px;font-weight:700;text-decoration:none;display:inline-flex;align-items:center;gap:8px;transition:all .15s;cursor:pointer;border:none}
.btn-gold{background:var(--gold);color:#1a1400}
.btn-gold:hover{background:var(--gold2);transform:translateY(-1px)}
.btn-ghost{background:transparent;color:var(--text);border:1px solid var(--line)}
.btn-ghost:hover{border-color:var(--purple);color:var(--purple-soft)}
.hero-note{font-size:12px;color:var(--muted2)}
.hero-note a{color:var(--purple-soft)}

/* TRUST BAR */
.trustbar{border-top:1px solid var(--line);border-bottom:1px solid var(--line);background:var(--navy2);padding:20px 24px}
.trust-items{max-width:860px;margin:0 auto;display:flex;justify-content:space-around;flex-wrap:wrap;gap:16px}
.trust-item{display:flex;align-items:center;gap:8px;font-size:12px;color:var(--muted)}
.trust-item i{color:var(--green);font-size:15px}

/* SCENARIOS SECTION */
.scenarios-section{padding:64px 24px}
.section-wrap{max-width:1040px;margin:0 auto}
.section-eyebrow{font-size:11px;color:var(--purple-soft);text-transform:uppercase;letter-spacing:0.12em;font-weight:600;margin-bottom:8px}
.section-title{font-size:clamp(22px,3.5vw,34px);font-weight:700;margin-bottom:8px;letter-spacing:-0.01em}
.section-sub{font-size:14px;color:var(--muted);margin-bottom:36px;max-width:520px}
.scenarios-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:16px}

/* SCENARIO CARD */
.sc-card{background:var(--navy3);border:1px solid var(--line);border-radius:var(--radius);padding:22px;position:relative;cursor:pointer;transition:border-color .2s,transform .15s}
.sc-card:hover{border-color:rgba(108,99,255,0.5);transform:translateY(-2px)}
.sc-card.locked{opacity:0.6;cursor:default}
.sc-card.locked:hover{transform:none;border-color:var(--line)}
.sc-card.completed{border-color:var(--green)}
.sc-header{display:flex;align-items:flex-start;justify-content:space-between;margin-bottom:12px}
.sc-icon{width:42px;height:42px;border-radius:10px;display:flex;align-items:center;justify-content:center;font-size:20px;flex-shrink:0}
.sc-badge{font-size:10px;font-weight:600;padding:3px 10px;border-radius:20px;display:flex;align-items:center;gap:4px}
.badge-free{background:rgba(29,158,117,0.15);color:var(--green);border:1px solid rgba(29,158,117,0.3)}
.badge-pro{background:rgba(108,99,255,0.15);color:var(--purple-soft);border:1px solid rgba(108,99,255,0.3)}
.badge-done{background:rgba(29,158,117,0.2);color:var(--green);border:1px solid rgba(29,158,117,0.4)}
.sc-num{font-size:10px;color:var(--muted2);font-weight:600;letter-spacing:0.08em;margin-bottom:4px}
.sc-title{font-size:15px;font-weight:600;color:var(--text);margin-bottom:6px}
.sc-desc{font-size:12px;color:var(--muted);line-height:1.65;margin-bottom:14px}
.sc-meta{display:flex;align-items:center;gap:12px}
.sc-time{font-size:11px;color:var(--muted2);display:flex;align-items:center;gap:4px}
.sc-time i{font-size:12px}
.sc-skills{display:flex;gap:5px;flex-wrap:wrap}
.skill-tag{font-size:10px;padding:2px 8px;border-radius:20px;background:var(--navy4);color:var(--muted);border:1px solid var(--line)}
.sc-lock{position:absolute;top:16px;right:16px;font-size:16px;color:var(--muted2)}
.sc-progress-bar{height:3px;background:var(--navy4);border-radius:2px;margin-top:14px;overflow:hidden}
.sc-progress-fill{height:100%;background:var(--green);border-radius:2px;transition:width .3s}

/* MODAL */
.modal-overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,0.85);z-index:200;align-items:center;justify-content:center;padding:20px}
.modal-overlay.open{display:flex}
.modal{background:var(--navy2);border:1px solid var(--line);border-radius:16px;width:100%;max-width:820px;max-height:90vh;overflow-y:auto;position:relative}
.modal-header{padding:24px 28px;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;position:sticky;top:0;background:var(--navy2);z-index:10}
.modal-title-area{display:flex;align-items:center;gap:14px}
.modal-icon{width:44px;height:44px;border-radius:10px;display:flex;align-items:center;justify-content:center;font-size:22px;flex-shrink:0}
.modal-title{font-size:18px;font-weight:700}
.modal-scenario-num{font-size:11px;color:var(--muted);margin-top:2px}
.modal-close{background:var(--navy3);border:1px solid var(--line);color:var(--muted);width:36px;height:36px;border-radius:8px;display:flex;align-items:center;justify-content:center;cursor:pointer;font-size:16px;transition:all .15s}
.modal-close:hover{border-color:var(--red);color:var(--red)}
.modal-body{padding:28px}

/* SCENARIO STEPS */
.scenario-brief{background:var(--navy3);border:1px solid var(--line);border-radius:10px;padding:16px 18px;margin-bottom:24px}
.brief-label{font-size:10px;color:var(--gold);font-weight:700;letter-spacing:0.1em;text-transform:uppercase;margin-bottom:6px}
.brief-text{font-size:13px;color:var(--muted);line-height:1.7}
.steps-list{display:flex;flex-direction:column;gap:0}
.step-item{display:flex;gap:16px;padding:16px 0;border-bottom:1px solid var(--line)}
.step-item:last-child{border-bottom:none}
.step-number{flex:0 0 32px;height:32px;border-radius:50%;background:var(--navy3);border:1px solid var(--line);display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;color:var(--muted);transition:all .3s;margin-top:2px}
.step-number.active{background:rgba(108,99,255,0.2);border-color:var(--purple);color:var(--purple-soft)}
.step-number.done{background:rgba(29,158,117,0.2);border-color:var(--green);color:var(--green)}
.step-content{flex:1}
.step-title{font-size:14px;font-weight:600;color:var(--text);margin-bottom:4px}
.step-desc{font-size:12px;color:var(--muted);line-height:1.65;margin-bottom:10px}
.step-action{display:none}
.step-action.visible{display:block}

/* INBOX SIMULATION */
.inbox-list{display:flex;flex-direction:column;gap:6px;margin:10px 0}
.email-row{background:var(--navy3);border:1px solid var(--line);border-radius:8px;padding:10px 14px;display:flex;align-items:center;gap:12px;cursor:pointer;transition:border-color .15s}
.email-row:hover{border-color:rgba(108,99,255,0.4)}
.email-row.actioned{opacity:0.5}
.email-row.actioned .er-action{display:flex}
.er-sender{font-size:12px;font-weight:600;color:var(--text);min-width:120px}
.er-subject{font-size:12px;color:var(--muted);flex:1}
.er-tag{font-size:10px;padding:2px 8px;border-radius:20px;white-space:nowrap}
.tag-urgent{background:rgba(226,75,74,0.15);color:var(--red);border:1px solid rgba(226,75,74,0.3)}
.tag-routine{background:rgba(55,138,221,0.15);color:var(--blue);border:1px solid rgba(55,138,221,0.3)}
.tag-ceo{background:rgba(240,180,41,0.15);color:var(--gold);border:1px solid rgba(240,180,41,0.3)}
.tag-archive{background:var(--navy4);color:var(--muted2);border:1px solid var(--line)}
.er-action{display:none;align-items:center;gap:5px;font-size:11px;color:var(--green)}
.action-btns{display:flex;gap:6px;margin-top:6px;flex-wrap:wrap}
.action-btn{font-size:11px;padding:4px 10px;border-radius:6px;cursor:pointer;border:none;font-weight:500;transition:all .15s}
.ab-reply{background:rgba(29,158,117,0.15);color:var(--green);border:1px solid rgba(29,158,117,0.3)}
.ab-reply:hover{background:rgba(29,158,117,0.25)}
.ab-ceo{background:rgba(240,180,41,0.15);color:var(--gold);border:1px solid rgba(240,180,41,0.3)}
.ab-ceo:hover{background:rgba(240,180,41,0.25)}
.ab-archive{background:var(--navy4);color:var(--muted);border:1px solid var(--line)}
.ab-archive:hover{border-color:var(--muted2)}
.ab-draft{background:rgba(55,138,221,0.15);color:var(--blue);border:1px solid rgba(55,138,221,0.3)}
.ab-draft:hover{background:rgba(55,138,221,0.25)}
.feedback-msg{font-size:11px;padding:6px 10px;border-radius:6px;margin-top:6px;display:none}
.feedback-ok{background:rgba(29,158,117,0.1);color:var(--green);border:1px solid rgba(29,158,117,0.2)}
.feedback-warn{background:rgba(240,180,41,0.1);color:var(--gold);border:1px solid rgba(240,180,41,0.2)}

/* TASK BUILDER */
.task-pool{display:flex;flex-direction:column;gap:6px;margin:10px 0}
.task-item{background:var(--navy3);border:1px solid var(--line);border-radius:8px;padding:9px 14px;display:flex;align-items:center;gap:10px;cursor:pointer;transition:all .15s;font-size:12px}
.task-item:hover{border-color:rgba(108,99,255,0.4)}
.task-item.selected{border-color:var(--purple);background:rgba(108,99,255,0.08)}
.task-cb{width:16px;height:16px;border-radius:4px;border:1px solid var(--line);display:flex;align-items:center;justify-content:center;font-size:10px;flex-shrink:0;transition:all .15s}
.task-item.selected .task-cb{background:var(--purple);border-color:var(--purple);color:#fff}
.task-priority{font-size:10px;padding:2px 7px;border-radius:20px;margin-left:auto}
.pri-high{background:rgba(226,75,74,0.15);color:var(--red)}
.pri-med{background:rgba(240,180,41,0.15);color:var(--gold)}
.pri-low{background:var(--navy4);color:var(--muted2)}

/* BRIEFING OUTPUT */
.briefing-output{background:var(--navy);border:1px solid var(--purple);border-radius:10px;padding:18px;font-size:12px;line-height:1.75;display:none}
.briefing-output.visible{display:block}
.bo-section{margin-bottom:14px}
.bo-label{font-size:10px;color:var(--gold);font-weight:700;text-transform:uppercase;letter-spacing:0.1em;margin-bottom:5px}
.bo-content{color:var(--muted);padding-left:12px;border-left:2px solid var(--line)}

/* CALENDAR SCENARIO */
.cal-conflicts{display:flex;flex-direction:column;gap:10px;margin:10px 0}
.conflict-card{background:var(--navy3);border:1px solid var(--red);border-radius:8px;padding:14px;position:relative}
.conflict-label{font-size:10px;color:var(--red);font-weight:700;text-transform:uppercase;letter-spacing:0.08em;margin-bottom:8px;display:flex;align-items:center;gap:5px}
.conflict-meetings{display:flex;flex-direction:column;gap:6px;margin-bottom:10px}
.cm-row{background:var(--navy);border:1px solid var(--line);border-radius:6px;padding:8px 12px;font-size:11px}
.cm-title{color:var(--text);font-weight:500;margin-bottom:2px}
.cm-meta{color:var(--muted2)}
.conflict-resolve{display:flex;gap:6px;flex-wrap:wrap}
.resolve-btn{font-size:11px;padding:5px 12px;border-radius:6px;cursor:pointer;border:none;font-weight:500;background:rgba(108,99,255,0.15);color:var(--purple-soft);border:1px solid rgba(108,99,255,0.3);transition:all .15s}
.resolve-btn:hover{background:rgba(108,99,255,0.25)}
.conflict-card.resolved{border-color:var(--green)}
.conflict-card.resolved .conflict-label{color:var(--green)}

/* PROGRESS SIDEBAR */
.progress-sidebar{background:var(--navy3);border:1px solid var(--line);border-radius:10px;padding:16px;margin-bottom:24px}
.ps-title{font-size:12px;font-weight:600;color:var(--text);margin-bottom:12px;display:flex;align-items:center;gap:6px}
.ps-title i{color:var(--gold)}
.ps-steps{display:flex;flex-direction:column;gap:6px}
.ps-step{display:flex;align-items:center;gap:8px;font-size:11px;color:var(--muted2)}
.ps-step.active{color:var(--text)}
.ps-step.done{color:var(--green)}
.ps-dot{width:6px;height:6px;border-radius:50%;background:var(--line);flex-shrink:0}
.ps-step.active .ps-dot{background:var(--purple)}
.ps-step.done .ps-dot{background:var(--green)}

/* BADGE EARNED */
.badge-earned{background:linear-gradient(135deg,rgba(29,158,117,0.15),rgba(108,99,255,0.1));border:1px solid rgba(29,158,117,0.4);border-radius:12px;padding:24px;text-align:center;display:none;margin-top:20px}
.badge-earned.visible{display:block}
.be-icon{font-size:48px;margin-bottom:10px}
.be-title{font-size:18px;font-weight:700;color:var(--green);margin-bottom:6px}
.be-desc{font-size:13px;color:var(--muted);margin-bottom:16px}
.be-actions{display:flex;gap:10px;justify-content:center;flex-wrap:wrap}

/* PIPELINE SECTION */
.pipeline-section{background:var(--navy2);border-top:1px solid var(--line);border-bottom:1px solid var(--line);padding:64px 24px}
.pipeline{display:flex;align-items:center;justify-content:center;gap:0;flex-wrap:wrap;max-width:860px;margin:40px auto 0}
.pipeline-step{text-align:center;padding:16px 20px;flex:1;min-width:140px}
.pip-icon{width:52px;height:52px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:22px;margin:0 auto 10px;border:2px solid var(--line)}
.pip-icon.step1{background:rgba(108,99,255,0.15);border-color:var(--purple);color:var(--purple-soft)}
.pip-icon.step2{background:rgba(240,180,41,0.15);border-color:var(--gold);color:var(--gold)}
.pip-icon.step3{background:rgba(29,158,117,0.15);border-color:var(--green);color:var(--green)}
.pip-icon.step4{background:rgba(55,138,221,0.15);border-color:var(--blue);color:var(--blue)}
.pip-title{font-size:13px;font-weight:600;color:var(--text);margin-bottom:4px}
.pip-sub{font-size:11px;color:var(--muted)}
.pip-arrow{font-size:20px;color:var(--line);padding:0 4px;align-self:center}

/* BOTTOM CTA */
.cta-section{padding:72px 24px;text-align:center}
.cta-section h2{font-size:clamp(24px,4vw,38px);font-weight:700;margin-bottom:12px;letter-spacing:-0.01em}
.cta-section p{font-size:14px;color:var(--muted);margin-bottom:28px}

/* PROGRESS BAR TOP */
.progress-top{position:fixed;top:0;left:0;right:0;height:3px;background:var(--line);z-index:200}
.progress-top-fill{height:100%;background:linear-gradient(90deg,var(--purple),var(--gold));transition:width .4s;width:0%}

/* OVERALL PROGRESS CARD */
.overall-progress{background:var(--navy3);border:1px solid var(--line);border-radius:var(--radius);padding:20px 24px;margin-bottom:32px;display:flex;align-items:center;gap:20px;flex-wrap:wrap}
.op-label{font-size:12px;color:var(--muted);min-width:100px}
.op-bar{flex:1;height:8px;background:var(--navy4);border-radius:4px;overflow:hidden;min-width:120px}
.op-fill{height:100%;background:linear-gradient(90deg,var(--purple),var(--green));border-radius:4px;transition:width .4s}
.op-count{font-size:13px;font-weight:700;color:var(--text);white-space:nowrap}
.op-badges{display:flex;gap:6px;flex-wrap:wrap}
.mini-badge{width:28px;height:28px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:13px;border:2px solid var(--line);background:var(--navy4);filter:grayscale(1);opacity:0.4;transition:all .3s}
.mini-badge.earned{filter:none;opacity:1;border-color:var(--green)}

/* FOOTER */
footer{border-top:1px solid var(--line);padding:32px 24px;text-align:center}
.footer-logo{color:var(--gold);font-weight:700;font-size:15px;margin-bottom:6px}
.footer-sub{font-size:11px;color:var(--muted2);line-height:1.8}
.footer-links{display:flex;justify-content:center;gap:16px;margin-top:10px;flex-wrap:wrap}
.footer-links a{color:var(--muted2);font-size:11px;text-decoration:none}
.footer-links a:hover{color:var(--muted)}

@media(max-width:640px){
  .pipeline{flex-direction:column}.pip-arrow{transform:rotate(90deg)}
  .overall-progress{flex-direction:column;align-items:flex-start}
}
</style>
</head>
<body>

<div class="progress-top"><div class="progress-top-fill" id="progressTop"></div></div>

<!-- NAV -->
<nav>
  <a href="https://getnexusos.app" class="nav-logo">⚡ NexusOS <span>EA Workflow Lab</span></a>
  <div class="nav-right">
    <span style="color:var(--muted);font-size:13px;cursor:default">VettedEA — Coming Soon</span>
    <a href="https://getnexusos.app" class="btn-nav">Open NexusOS →</a>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-badge"><i class="ti ti-school"></i> Free Training Simulator · Built by a Working EA</div>
  <h1>Practice the execution.<br><em>Not just the theory.</em></h1>
  <p class="hero-sub">Real EA scenarios. Real NexusOS tool. Real pressure — zero real clients at risk. Train on 8 core executive workflows and earn your EA Workflow Lab certification before your first client is watching.</p>
  <div class="hero-ctas">
    <a href="#scenarios" class="btn-lg btn-gold"><i class="ti ti-player-play"></i> Start training — free</a>
    <span class="btn-lg btn-ghost" style="opacity:0.45;cursor:default"><i class="ti ti-shield-check"></i> VettedEA — Coming Soon</span>
  </div>
  <p class="hero-note">Free for EAs, always. Badge earned here → fast-tracks your vetting on <strong style="color:#f0b429">VettedEA</strong> (coming soon).</p>
</section>

<!-- TRUST BAR -->
<div class="trustbar">
  <div class="trust-items">
    <div class="trust-item"><i class="ti ti-check"></i> 8 guided scenarios</div>
    <div class="trust-item"><i class="ti ti-check"></i> Realistic sandbox data</div>
    <div class="trust-item"><i class="ti ti-check"></i> Self-paced or guided mode</div>
    <div class="trust-item"><i class="ti ti-check"></i> Completion badge for VettedEA</div>
    <div class="trust-item"><i class="ti ti-check"></i> Built by a working EA</div>
    <div class="trust-item"><i class="ti ti-check"></i> Free to start</div>
  </div>
</div>

<!-- SCENARIOS -->
<section class="scenarios-section" id="scenarios">
  <div class="section-wrap">
    <div class="section-eyebrow">Training Scenarios</div>
    <h2 class="section-title">8 core EA workflows.</h2>
    <p class="section-sub">Start with the 2 free scenarios. Complete all 8 to earn your EA Workflow Lab Certified badge.</p>

    <!-- OVERALL PROGRESS -->
    <div class="overall-progress">
      <div class="op-label">Your progress</div>
      <div class="op-bar"><div class="op-fill" id="overallFill" style="width:0%"></div></div>
      <div class="op-count" id="overallCount">0 / 8 complete</div>
      <div class="op-badges" id="badgeRow">
        <div class="mini-badge" id="mb0" title="Inbox Zero">📥</div>
        <div class="mini-badge" id="mb1" title="Monday Briefing">📋</div>
        <div class="mini-badge" id="mb2" title="Calendar Crisis">📅</div>
        <div class="mini-badge" id="mb3" title="Stakeholder Tracker">✅</div>
        <div class="mini-badge" id="mb4" title="Meeting Prep">🗂</div>
        <div class="mini-badge" id="mb5" title="Vendor Approvals">⚡</div>
        <div class="mini-badge" id="mb6" title="Travel Coordinator">✈️</div>
        <div class="mini-badge" id="mb7" title="CRM Cleanup">📊</div>
      </div>
    </div>

    <div class="scenarios-grid" id="scenariosGrid">
      <!-- Cards rendered by JS -->
    </div>
  </div>
</section>

<!-- PIPELINE -->
<section class="pipeline-section">
  <div class="section-wrap" style="text-align:center">
    <div class="section-eyebrow">The full pipeline</div>
    <h2 class="section-title">Train → Certify → Get Hired</h2>
    <p class="section-sub" style="margin:0 auto">EA Workflow Lab is the first step in a complete ecosystem. Complete the lab and your certification feeds directly into VettedEA.</p>
    <div class="pipeline">
      <div class="pipeline-step">
        <div class="pip-icon step1"><i class="ti ti-school"></i></div>
        <div class="pip-title">EA Workflow Lab</div>
        <div class="pip-sub">Practice 8 real EA scenarios on NexusOS</div>
      </div>
      <div class="pip-arrow">→</div>
      <div class="pipeline-step">
        <div class="pip-icon step2"><i class="ti ti-medal"></i></div>
        <div class="pip-title">Earn Your Badge</div>
        <div class="pip-sub">EA Workflow Lab Certified — valid everywhere on VettedEA</div>
      </div>
      <div class="pip-arrow">→</div>
      <div class="pipeline-step">
        <div class="pip-icon step3"><i class="ti ti-shield-check"></i></div>
        <div class="pip-title">Get Vetted</div>
        <div class="pip-sub">Fast-track vetting on VettedEA — coming soon</div>
      </div>
      <div class="pip-arrow">→</div>
      <div class="pipeline-step">
        <div class="pip-icon step4"><i class="ti ti-briefcase"></i></div>
        <div class="pip-title">Get Hired</div>
        <div class="pip-sub">Matched with verified employers. Keep 100% of your pay.</div>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="cta-section">
  <h2>Ready to train like you work?</h2>
  <p>Start with 3 free scenarios. Earn your badge. Get hired on VettedEA — coming soon.</p>
  <div style="display:flex;gap:12px;justify-content:center;flex-wrap:wrap">
    <a href="#scenarios" class="btn-lg btn-gold"><i class="ti ti-player-play"></i> Start training free</a>
    <span class="btn-lg btn-ghost" style="opacity:0.45;cursor:default"><i class="ti ti-shield-check"></i> VettedEA — Coming Soon</span>
  </div>
  <p style="margin-top:16px;font-size:12px;color:var(--muted2)">
    🚀 <strong style="color:var(--gold)">Coming soon:</strong> NexusOS launches on the Apple App Store and Google Play Store. Train anywhere, work anywhere.
  </p>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-logo">⚡ NexusOS · EA Workflow Lab</div>
  <div class="footer-sub">Built by Jhun Rey Velado · Working EA · HIPAA Certified · Founder of NexusOS + VettedEA<br>© 2026 All rights reserved</div>
  <div class="footer-links">
    <a href="https://getnexusos.app">NexusOS</a>
    <span style="color:var(--muted);cursor:default">VettedEA (coming soon)</span>
    <a href="https://linkedin.com/in/jhunreyvelado">LinkedIn</a>
    <a href="mailto:jhun07.velado@gmail.com">Contact</a>
  </div>
</footer>

<!-- SCENARIO MODAL -->
<div class="modal-overlay" id="scenarioModal">
  <div class="modal" id="modalContent">
    <div class="modal-header">
      <div class="modal-title-area">
        <div class="modal-icon" id="modalIcon"></div>
        <div>
          <div class="modal-title" id="modalTitle"></div>
          <div class="modal-scenario-num" id="modalNum"></div>
        </div>
      </div>
      <button class="modal-close" onclick="closeModal()"><i class="ti ti-x"></i></button>
    </div>
    <div class="modal-body" id="modalBody"></div>
  </div>
</div>

<script>
// ============================================================
// DATA
// ============================================================
const SCENARIOS = [
  {
    id: 0, free: true,
    icon: '📥', iconBg: 'rgba(55,138,221,0.15)', iconColor: '#378ADD',
    title: 'Inbox Zero Sprint',
    desc: 'Triage 15 realistic emails. Draft responses, flag CEO decisions, archive low-priority, batch non-urgents. Reach zero before your executive logs in.',
    time: '15–20 min', skills: ['Email triage','Prioritization','Drafting'],
    steps: ['Read the situation','Triage all 15 emails','Flag CEO decisions','Reach Inbox Zero','Review your briefing'],
    build: buildInboxScenario
  },
  {
    id: 1, free: true,
    icon: '📋', iconBg: 'rgba(240,180,41,0.15)', iconColor: '#f0b429',
    title: 'Monday Briefing Build',
    desc: 'Review pending tasks, overdue items, and the week\'s calendar. Select priorities, add context, and generate the Monday morning briefing before 8am.',
    time: '15–20 min', skills: ['Briefing','Task review','Calendar prep'],
    steps: ['Review pending tasks','Select top priorities','Add meeting context','Generate briefing','Send to executive'],
    build: buildBriefingScenario
  },
  {
    id: 2, free: true,
    icon: '📅', iconBg: 'rgba(226,75,74,0.15)', iconColor: '#E24B4A',
    title: 'Calendar Crisis',
    desc: '3 scheduling conflicts. 4 time zones. Resolve every clash without a double-booking — and without disturbing your CEO\'s focus time.',
    time: '15–20 min', skills: ['Calendar management','Time zones','Communication'],
    steps: ['Identify all conflicts','Check time zones','Propose reschedule','Confirm with parties','Update calendar'],
    build: buildCalendarScenario
  },
  {
    id: 3, free: false,
    icon: '✅', iconBg: 'rgba(29,158,117,0.15)', iconColor: '#1D9E75',
    title: 'Stakeholder Tracker',
    desc: '40 open action items across 8 stakeholders. Nothing can drop. Tag, assign, and set follow-up dates for every single item.',
    time: '20–25 min', skills: ['Task management','Follow-up','CRM'],
    steps: ['Import action items','Assign owners','Set due dates','Flag overdue','Generate status report'],
    build: buildStakeholderScenario
  },
  {
    id: 4, free: false,
    icon: '🗂', iconBg: 'rgba(108,99,255,0.15)', iconColor: '#8B84FF',
    title: 'Meeting Prep Sprint',
    desc: '5 back-to-back meetings. For each one: attendees, agenda, required documents, and a 3-line CEO briefing note. All done before 9am.',
    time: '20–25 min', skills: ['Meeting prep','Research','Briefing notes'],
    steps: ['Pull meeting list','Research each attendee','Build agendas','Write CEO brief notes','Package prep kit'],
    build: buildMeetingPrepScenario
  },
  {
    id: 5, free: false,
    icon: '⚡', iconBg: 'rgba(240,180,41,0.15)', iconColor: '#f0b429',
    title: 'Vendor Approval Chain',
    desc: '4 vendors waiting on decisions. Decide what you can approve, what needs CEO eyes, and what should be rejected — with clear reasoning for each.',
    time: '15 min', skills: ['Approvals','Vendor management','Decision-making'],
    steps: ['Review all 4 requests','Check approval authority','Process what you can','Escalate CEO items','Log decisions'],
    build: buildApprovalScenario
  },
  {
    id: 6, free: false,
    icon: '✈️', iconBg: 'rgba(55,138,221,0.15)', iconColor: '#378ADD',
    title: 'Travel Coordination',
    desc: 'Book a 3-city business trip: flights, hotels, ground transport, schedule blocks, and a full itinerary document — ready for your CEO to print.',
    time: '20–25 min', skills: ['Travel booking','Itinerary building','Calendar management'],
    steps: ['Get trip requirements','Book transport legs','Reserve hotels','Build itinerary doc','Block calendar + notify'],
    build: buildTravelScenario
  },
  {
    id: 7, free: false,
    icon: '📊', iconBg: 'rgba(29,158,117,0.15)', iconColor: '#1D9E75',
    title: 'CRM Cleanup',
    desc: '20 stale contact records. Update relationship notes, log last contact dates, flag dead leads, and make the CRM something your CEO will actually trust.',
    time: '15–20 min', skills: ['CRM management','Data hygiene','Relationship tracking'],
    steps: ['Audit all 20 contacts','Update relationship notes','Log activity dates','Flag dead/inactive leads','Generate clean report'],
    build: buildCRMScenario
  }
];

// ============================================================
// PROGRESS
// ============================================================
function getProgress(){
  try{ return JSON.parse(localStorage.getItem('eawl_progress')||'{}'); }
  catch(e){ return {}; }
}
function saveProgress(p){ localStorage.setItem('eawl_progress', JSON.stringify(p)); }
function markComplete(id){
  const p = getProgress(); p[id] = true; saveProgress(p);
  updateOverallProgress();
}
function isComplete(id){ return !!getProgress()[id]; }

function updateOverallProgress(){
  const p = getProgress();
  const done = Object.values(p).filter(Boolean).length;
  const pct = Math.round((done/8)*100);
  document.getElementById('overallFill').style.width = pct+'%';
  document.getElementById('overallCount').textContent = done+' / 8 complete';
  document.getElementById('progressTop').querySelector('.progress-top-fill').style.width = pct+'%';
  SCENARIOS.forEach(s=>{
    const mb = document.getElementById('mb'+s.id);
    if(mb) mb.classList.toggle('earned', isComplete(s.id));
  });
  renderCards();
}

// ============================================================
// RENDER SCENARIO CARDS
// ============================================================
function renderCards(){
  const grid = document.getElementById('scenariosGrid');
  grid.innerHTML = SCENARIOS.map(s => {
    const done = isComplete(s.id);
    const locked = !s.free && !done && !isPaidUser();
    return `
    <div class="sc-card ${locked?'locked':''} ${done?'completed':''}" 
         onclick="${locked ? 'showUpgrade()' : `openScenario(${s.id})`}">
      ${locked ? '<div class="sc-lock"><i class="ti ti-lock"></i></div>' : ''}
      <div class="sc-header">
        <div class="sc-icon" style="background:${s.iconBg};color:${s.iconColor}">${s.icon}</div>
        <div class="sc-badge ${done ? 'badge-done' : s.free ? 'badge-free' : 'badge-pro'}">
          ${done ? '<i class="ti ti-check"></i> Done' : s.free ? 'Free' : '🔒 Pro'}
        </div>
      </div>
      <div class="sc-num">SCENARIO ${String(s.id+1).padStart(2,'0')}</div>
      <div class="sc-title">${s.title}</div>
      <div class="sc-desc">${s.desc}</div>
      <div class="sc-meta">
        <div class="sc-time"><i class="ti ti-clock"></i> ${s.time}</div>
        <div class="sc-skills">${s.skills.map(sk=>`<span class="skill-tag">${sk}</span>`).join('')}</div>
      </div>
      <div class="sc-progress-bar"><div class="sc-progress-fill" style="width:${done?'100%':'0%'}"></div></div>
    </div>`;
  }).join('');
}

function isPaidUser(){
  var params = new URLSearchParams(window.location.search);
  if(params.get('unlock') === 'EAWL29PRO'){
    localStorage.setItem('eawl_paid','true');
    window.history.replaceState({},'',window.location.pathname);
  }
  return localStorage.getItem('eawl_paid') === 'true';
}

function showUpgrade(){ /* handled inline */ }

function activatePro(){
  localStorage.setItem('eawl_paid','true');
  location.reload();
}

// ============================================================
// MODAL
// ============================================================
function openScenario(id){
  const s = SCENARIOS[id];
  const modal = document.getElementById('scenarioModal');
  document.getElementById('modalIcon').style.cssText = `background:${s.iconBg};color:${s.iconColor};font-size:22px`;
  document.getElementById('modalIcon').textContent = s.icon;
  document.getElementById('modalTitle').textContent = s.title;
  document.getElementById('modalNum').textContent = `Scenario ${id+1} of 8 · ${s.time}`;
  document.getElementById('modalBody').innerHTML = s.build(s, id);
  modal.classList.add('open');
  document.body.style.overflow = 'hidden';
}
function closeModal(){
  document.getElementById('scenarioModal').classList.remove('open');
  document.body.style.overflow = '';
  updateOverallProgress();
}
document.getElementById('scenarioModal').addEventListener('click', function(e){
  if(e.target === this) closeModal();
});

// ============================================================
// SCENARIO 1: INBOX ZERO
// ============================================================
const EMAILS = [
  {sender:'Sarah Chen (Board)',subject:'Q3 Board Deck — final review needed',tag:'ceo',tagLabel:'Flag CEO',correct:'ceo',feedback:'✓ Correct! Board requests always go to your CEO with context. Flag and add to their priority list.'},
  {sender:'Vendor: SupplyMax',subject:'Invoice #4421 — payment overdue',tag:'routine',tagLabel:'Routine',correct:'draft',feedback:'✓ Good call. Draft a response acknowledging receipt and confirm payment timeline with finance.'},
  {sender:'Mark Rivera (CFO)',subject:'Budget approval needed by EOD',tag:'urgent',tagLabel:'Urgent',correct:'ceo',feedback:'✓ Correct. Budget approvals above your authority threshold go to CEO immediately — this is time-sensitive.'},
  {sender:'LinkedIn Notifications',subject:'You have 12 new connections this week',tag:'archive',tagLabel:'Low priority',correct:'archive',feedback:'✓ Correct. Non-actionable notifications are archived immediately. Never let them sit as unread.'},
  {sender:'James Okonkwo',subject:'Following up on our call last Tuesday',tag:'routine',tagLabel:'Routine',correct:'draft',feedback:'✓ Draft a professional follow-up on behalf of the CEO. Include a summary of the call action items.'},
  {sender:'IT Support',subject:'Your password expires in 3 days',tag:'routine',tagLabel:'Routine',correct:'draft',feedback:'✓ Handle this yourself — schedule a 5-minute password reset so it never becomes urgent.'},
  {sender:'Ana Petrov (Investor)',subject:'Interested in a catch-up call this month',tag:'ceo',tagLabel:'Flag CEO',correct:'ceo',feedback:'✓ Investor relationship — always surface to CEO. Propose 3 time slots and await CEO preference before confirming.'},
  {sender:'Newsletter: Forbes',subject:'Top 10 Business Leaders to Watch',tag:'archive',tagLabel:'Low priority',correct:'archive',feedback:'✓ External newsletters are archived unless they contain relevant mentions of your CEO or company.'},
  {sender:'Conference: WebSummit',subject:'Speaker application deadline: Friday',tag:'ceo',tagLabel:'Flag CEO',correct:'ceo',feedback:'✓ Opportunities like this need CEO awareness quickly. Flag with deadline highlighted.'},
  {sender:'Office Manager',subject:'Kitchen supplies order — approve list?',tag:'routine',tagLabel:'Routine',correct:'draft',feedback:'✓ Operational requests within your authority are handled by you directly. Draft approval and handle.'},
  {sender:'Priya Nair (Client)',subject:'Still waiting on the revised proposal',tag:'urgent',tagLabel:'Urgent',correct:'draft',feedback:'✓ Client follow-up is urgent. Draft a holding response immediately and flag internally to get the proposal moving.'},
  {sender:'Amazon Business',subject:'Your monthly account statement',tag:'archive',tagLabel:'Low priority',correct:'archive',feedback:'✓ Routine statements are archived in the relevant folder. Flag for monthly reconciliation review.'},
  {sender:'Legal: Morris & Co.',subject:'NDA draft ready for CEO signature',tag:'ceo',tagLabel:'Flag CEO',correct:'ceo',feedback:'✓ Documents requiring CEO signature are escalated immediately with the document attached for review.'},
  {sender:'Team Slack Digest',subject:'Daily digest from #general channel',tag:'archive',tagLabel:'Low priority',correct:'archive',feedback:'✓ Digest emails are archived. You monitor Slack directly — these create double noise.'},
  {sender:'Recruitment: TopTalent',subject:'2 strong candidates for the Ops Manager role',tag:'ceo',tagLabel:'Flag CEO',correct:'ceo',feedback:'✓ Hiring decisions surface to CEO with a brief on each candidate. Don\'t let recruitment decisions stall in your inbox.'}
];

function buildInboxScenario(s, id){
  const emailRows = EMAILS.map((e,i) => `
    <div class="email-row" id="er${i}">
      <div class="er-sender">${e.sender}</div>
      <div class="er-subject">${e.subject}</div>
      <span class="er-tag tag-${e.tag}">${e.tagLabel}</span>
      <div class="er-action" id="era${i}"><i class="ti ti-check" style="color:var(--green)"></i> Actioned</div>
    </div>
    <div id="ebody${i}" style="display:none;padding:0 0 8px 0">
      <div class="action-btns">
        <button class="action-btn ab-ceo" onclick="emailAction(${i},'ceo')">🏆 Flag for CEO</button>
        <button class="action-btn ab-reply" onclick="emailAction(${i},'draft')">✏️ Draft Response</button>
        <button class="action-btn ab-archive" onclick="emailAction(${i},'archive')">📁 Archive</button>
      </div>
      <div class="feedback-msg" id="fb${i}"></div>
    </div>
  `).join('');

  return `
    <div class="scenario-brief">
      <div class="brief-label">📌 Situation</div>
      <div class="brief-text">It's Monday 7:15am. Alex Rivera's inbox has 15 emails that arrived over the weekend. Your CEO has a board call at 9am. You have 45 minutes to reach inbox zero — triage, draft, flag, and archive every email before they open their laptop. Click each email to take action.</div>
    </div>
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:10px">
      <div style="font-size:12px;color:var(--muted)">Click an email to expand · Choose the right action</div>
      <div style="font-size:12px;color:var(--text);font-weight:600">
        <span id="inboxCount">0</span>/15 actioned
      </div>
    </div>
    <div class="inbox-list">${emailRows}</div>
    <div class="badge-earned" id="inboxBadge">
      <div class="be-icon">📥</div>
      <div class="be-title">Inbox Zero — Achieved!</div>
      <div class="be-desc">You triaged 15 emails with zero left in the inbox. Every CEO decision surfaced. Every routine handled. Every archive cleared. That's what real executive support looks like.</div>
      <div class="be-actions">
        <button class="btn-lg btn-gold" onclick="completeScenario(${id})">✓ Claim Your Badge</button>
        <a href="#scenarios" class="btn-lg btn-ghost" onclick="closeModal()">Next scenario →</a>
      </div>
    </div>
  `;
}

function toggleEmail(i){
  const body = document.getElementById('ebody'+i);
  body.style.display = body.style.display==='none'?'block':'none';
}

let emailsActioned = 0;
function emailAction(i, action){
  const e = EMAILS[i];
  const fb = document.getElementById('fb'+i);
  const row = document.getElementById('er'+i);
  fb.textContent = e.feedback;
  fb.className = 'feedback-msg '+(action===e.correct?'feedback-ok':'feedback-warn');
  fb.style.display = 'block';
  if(!row.classList.contains('actioned')){
    row.classList.add('actioned');
    document.getElementById('era'+i).style.display='flex';
    emailsActioned++;
    document.getElementById('inboxCount').textContent = emailsActioned;
    if(emailsActioned >= 15){
      document.getElementById('inboxBadge').classList.add('visible');
    }
  }
}

// Click email rows
document.addEventListener('click', function(e){
  const row = e.target.closest('.email-row');
  if(row){
    const id = row.id.replace('er','');
    toggleEmail(parseInt(id));
  }
});

// ============================================================
// SCENARIO 2: MONDAY BRIEFING
// ============================================================
const TASKS = [
  {text:'Prepare Q3 investor update deck',priority:'high',include:true,reason:'Board-level deliverable due this week — must be in the briefing.'},
  {text:'Follow up with Chen Law on NDA',priority:'high',include:true,reason:'Legal follow-up that directly impacts a pending deal — include.'},
  {text:'Order new printer cartridges',priority:'low',include:false,reason:'Operational task handled by you — no CEO attention needed.'},
  {text:'Review and approve revised budget',priority:'high',include:true,reason:'Financial approval — CEO must action this today.'},
  {text:'Call back Ana Petrov (investor)',priority:'high',include:true,reason:'Investor relationship — CEO needs context before the callback.'},
  {text:'Book team lunch reservation',priority:'low',include:false,reason:'Admin task — handled by you. Not a CEO priority item.'},
  {text:'Send signed contract to Meridian Group',priority:'high',include:true,reason:'Client deliverable — time-sensitive. CEO should know status.'},
  {text:'Update LinkedIn company page',priority:'low',include:false,reason:'Marketing admin — not a CEO Monday morning priority.'},
  {text:'Confirm keynote speaker slot for Oct conference',priority:'med',include:true,reason:'Has a deadline — CEO needs to know and confirm.'},
  {text:'Review WebSummit speaker proposal',priority:'med',include:true,reason:'Opportunity with a deadline — CEO decision needed soon.'}
];

const MEETINGS = [
  {time:'9:00 AM',title:'Board Strategy Call',attendees:'Sarah Chen, Michael Ross, David Kim',prep:'Q3 deck, board agenda, key decisions list'},
  {time:'11:30 AM',title:'Investor Call — Ana Petrov',attendees:'Ana Petrov (Horizon Capital)',prep:'Company update one-pager, Q2 metrics'},
  {time:'2:00 PM',title:'Legal Review — Morris & Co.',attendees:'James Morris, Team Legal',prep:'NDA draft, outstanding items list'},
  {time:'4:00 PM',title:'Team All-Hands',attendees:'Full team (24 people)',prep:'Agenda, Q3 goals slide, recognition notes'}
];

let selectedTasks = new Set();
let briefingGenerated = false;

function buildBriefingScenario(s, id){
  const taskRows = TASKS.map((t,i)=>`
    <div class="task-item" id="ti${i}" onclick="toggleTask(${i})">
      <div class="task-cb" id="tcb${i}"></div>
      <div style="flex:1;font-size:12px">${t.text}</div>
      <div class="task-priority ${t.priority==='high'?'pri-high':t.priority==='med'?'pri-med':'pri-low'}">${t.priority}</div>
    </div>
  `).join('');

  const meetingRows = MEETINGS.map(m=>`
    <div style="background:var(--navy3);border:1px solid var(--line);border-radius:8px;padding:10px 14px;margin-bottom:6px">
      <div style="display:flex;gap:10px;align-items:center">
        <div style="font-size:11px;color:var(--gold);font-weight:600;min-width:65px">${m.time}</div>
        <div style="font-size:12px;font-weight:600;color:var(--text);flex:1">${m.title}</div>
      </div>
      <div style="font-size:11px;color:var(--muted);margin-top:3px;padding-left:75px">${m.attendees}</div>
    </div>
  `).join('');

  return `
    <div class="scenario-brief">
      <div class="brief-label">📌 Situation</div>
      <div class="brief-text">It's Monday 6:50am. Your executive has 4 meetings before noon. You have 10 pending tasks in the system and need to build Alex Rivera's Monday briefing before they open their laptop at 8am. Select the items that belong in the CEO briefing, then generate it.</div>
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:16px">
      <div>
        <div style="font-size:11px;color:var(--gold);font-weight:700;text-transform:uppercase;letter-spacing:0.08em;margin-bottom:8px">📋 Pending Tasks — Select what belongs in CEO briefing</div>
        <div class="task-pool">${taskRows}</div>
        <div style="font-size:11px;color:var(--muted);margin-top:6px"><span id="selCount">0</span> selected</div>
      </div>
      <div>
        <div style="font-size:11px;color:var(--gold);font-weight:700;text-transform:uppercase;letter-spacing:0.08em;margin-bottom:8px">📅 Today's Calendar</div>
        ${meetingRows}
      </div>
    </div>
    <button class="btn-lg btn-gold" onclick="generateBriefing(${id})" style="margin-bottom:16px;font-size:13px;padding:10px 22px">
      <i class="ti ti-file-text"></i> Generate Monday Briefing
    </button>
    <div class="briefing-output" id="briefingOut"></div>
    <div class="badge-earned" id="briefingBadge" style="margin-top:16px">
      <div class="be-icon">📋</div>
      <div class="be-title">Monday Briefing — Delivered!</div>
      <div class="be-desc">Your executive wakes up to a clean, prioritized briefing with everything they need and nothing they don't. That's 30 minutes of CEO context recovered, automatically.</div>
      <div class="be-actions">
        <button class="btn-lg btn-gold" onclick="completeScenario(${id})">✓ Claim Your Badge</button>
        <a href="#scenarios" class="btn-lg btn-ghost" onclick="closeModal()">Next scenario →</a>
      </div>
    </div>
  `;
}

function toggleTask(i){
  const t = TASKS[i];
  if(selectedTasks.has(i)){ selectedTasks.delete(i); document.getElementById('ti'+i).classList.remove('selected'); document.getElementById('tcb'+i).textContent=''; }
  else { selectedTasks.add(i); document.getElementById('ti'+i).classList.add('selected'); document.getElementById('tcb'+i).textContent='✓'; }
  document.getElementById('selCount').textContent = selectedTasks.size;
}

function generateBriefing(scenarioId){
  const correct = TASKS.filter(t=>t.include);
  const wrongIncludes = [...selectedTasks].filter(i=>!TASKS[i].include);
  const missedItems = TASKS.filter((t,i)=>t.include && !selectedTasks.has(i));
  
  let feedback = '';
  if(missedItems.length > 0) feedback += `<div style="font-size:11px;color:var(--gold);margin-bottom:8px">⚠️ You missed ${missedItems.length} important item(s): ${missedItems.map(t=>t.text).join(', ')}. High-priority CEO items should always be included.</div>`;
  if(wrongIncludes.length > 0) feedback += `<div style="font-size:11px;color:var(--gold);margin-bottom:8px">⚠️ ${wrongIncludes.length} low-priority item(s) included that the CEO doesn't need to see. Keep briefings signal, not noise.</div>`;
  
  const selectedList = [...selectedTasks].map(i=>TASKS[i]);
  
  const out = document.getElementById('briefingOut');
  out.classList.add('visible');
  out.innerHTML = `
    ${feedback}
    <div style="border-bottom:1px solid var(--line);padding-bottom:10px;margin-bottom:12px">
      <div style="font-size:14px;font-weight:700;color:var(--text)">🌅 Good morning, Alex.</div>
      <div style="font-size:11px;color:var(--muted)">Monday, July 2026 · Prepared by Jhun · 7:45am</div>
    </div>
    <div class="bo-section">
      <div class="bo-label">🎯 Top priorities today</div>
      <div class="bo-content">
        ${selectedList.filter(t=>t.priority==='high').map(t=>`<div>• ${t.text}</div>`).join('') || '<div style="color:var(--muted2)">No high-priority items selected</div>'}
      </div>
    </div>
    <div class="bo-section">
      <div class="bo-label">📅 Your meetings today</div>
      <div class="bo-content">
        ${MEETINGS.map(m=>`<div>• <strong>${m.time}</strong> — ${m.title} · Prep: ${m.prep}</div>`).join('')}
      </div>
    </div>
    <div class="bo-section">
      <div class="bo-label">📋 Other items on your radar</div>
      <div class="bo-content">
        ${selectedList.filter(t=>t.priority!=='high').map(t=>`<div>• ${t.text}</div>`).join('') || '<div style="color:var(--muted2)">None selected</div>'}
      </div>
    </div>
    <div style="font-size:11px;color:var(--muted2);margin-top:12px;padding-top:10px;border-top:1px solid var(--line)">Prepared by your EA · NexusOS AI Briefing System · getnexusos.app</div>
  `;
  document.getElementById('briefingBadge').classList.add('visible');
}

// ============================================================
// SCENARIO 3: CALENDAR CRISIS
// ============================================================
function buildCalendarScenario(s, id){
  return `
    <div class="scenario-brief">
      <div class="brief-label">📌 Situation</div>
      <div class="brief-text">Three scheduling conflicts just appeared on Alex Rivera's calendar. You need to resolve all of them — no double-bookings, no disturbing the CEO's focus time, and no offending any of the parties. Click "Resolve" on each conflict to work through your options.</div>
    </div>
    <div class="cal-conflicts">
      <div class="conflict-card" id="cc0">
        <div class="conflict-label"><i class="ti ti-alert-triangle"></i> Conflict #1 — Double Booking</div>
        <div class="conflict-meetings">
          <div class="cm-row"><div class="cm-title">Board Strategy Call (Existing)</div><div class="cm-meta">Tuesday 10:00–11:30am EST · Sarah Chen, Michael Ross</div></div>
          <div class="cm-row"><div class="cm-title">Investor Call — Ana Petrov (New request)</div><div class="cm-meta">Tuesday 10:30am EST · Horizon Capital</div></div>
        </div>
        <div class="conflict-resolve">
          <button class="resolve-btn" onclick="resolveConflict(0,'A')">Move investor call to Wednesday</button>
          <button class="resolve-btn" onclick="resolveConflict(0,'B')">Ask CEO which to prioritize</button>
          <button class="resolve-btn" onclick="resolveConflict(0,'C')">Cancel the board call</button>
        </div>
        <div class="feedback-msg" id="ccfb0"></div>
      </div>
      <div class="conflict-card" id="cc1">
        <div class="conflict-label"><i class="ti ti-alert-triangle"></i> Conflict #2 — Focus Time Overlap</div>
        <div class="conflict-meetings">
          <div class="cm-row"><div class="cm-title">CEO Deep Work Block (Protected)</div><div class="cm-meta">Thursday 8:00–10:00am · DO NOT BOOK</div></div>
          <div class="cm-row"><div class="cm-title">Vendor Demo — CloudSuite (Requested)</div><div class="cm-meta">Thursday 9:00am · 45 min · Internal request from Ops team</div></div>
        </div>
        <div class="conflict-resolve">
          <button class="resolve-btn" onclick="resolveConflict(1,'A')">Decline demo, propose Thursday afternoon</button>
          <button class="resolve-btn" onclick="resolveConflict(1,'B')">Book it anyway — CEO can decide</button>
          <button class="resolve-btn" onclick="resolveConflict(1,'C')">Remove CEO deep work block</button>
        </div>
        <div class="feedback-msg" id="ccfb1"></div>
      </div>
      <div class="conflict-card" id="cc2">
        <div class="conflict-label"><i class="ti ti-alert-triangle"></i> Conflict #3 — Time Zone Error</div>
        <div class="conflict-meetings">
          <div class="cm-row"><div class="cm-title">Partner Call — Meridian Group (Dubai)</div><div class="cm-meta">Booked: Friday 3:00pm EST — Meridian team agreed to "3pm"</div></div>
          <div class="cm-row"><div class="cm-title">Issue: Both parties said "3pm" — different time zones</div><div class="cm-meta">3:00pm EST = 11:00pm Dubai time — that's 11pm for Meridian</div></div>
        </div>
        <div class="conflict-resolve">
          <button class="resolve-btn" onclick="resolveConflict(2,'A')">Clarify with Meridian, propose 10am EST (6pm Dubai)</button>
          <button class="resolve-btn" onclick="resolveConflict(2,'B')">Keep the booking — 11pm is fine for them</button>
          <button class="resolve-btn" onclick="resolveConflict(2,'C')">Cancel and let CEO handle it</button>
        </div>
        <div class="feedback-msg" id="ccfb2"></div>
      </div>
    </div>
    <div class="badge-earned" id="calBadge">
      <div class="be-icon">📅</div>
      <div class="be-title">Calendar Crisis — Resolved!</div>
      <div class="be-desc">3 conflicts. Zero double-bookings. CEO focus time protected. Every party handled professionally. You just saved your executive from 3 awkward conversations.</div>
      <div class="be-actions">
        <button class="btn-lg btn-gold" onclick="completeScenario(${id})">✓ Claim Your Badge</button>
        <a href="#scenarios" class="btn-lg btn-ghost" onclick="closeModal()">Next scenario →</a>
      </div>
    </div>
  `;
}

const CONFLICT_ANSWERS = [
  { correct:'A', feedbacks:{
    A:'✓ Correct! Board calls are sacred. Move the investor call to Wednesday, notify Ana Petrov professionally with an apology and 3 alternative times.',
    B:'⚠️ Involving the CEO in a scheduling conflict is a last resort. Your job is to resolve this without disturbing their focus.',
    C:'✗ Never cancel an existing board meeting for a new request. Board commitments are the highest priority on any CEO calendar.'
  }},
  { correct:'A', feedbacks:{
    A:'✓ Perfect. CEO deep work blocks are protected by you. Decline the vendor demo during that window and propose Thursday afternoon — with a clear explanation to the Ops team.',
    B:'✗ You were given explicit instructions not to book during deep work time. Overriding a protected block is a serious EA mistake.',
    C:'✗ You never remove a CEO\'s protected time without their explicit instruction. Focus time is what allows them to do their best thinking.'
  }},
  { correct:'A', feedbacks:{
    A:'✓ Excellent catch. Always confirm time zones explicitly. 10am EST / 6pm Dubai is a reasonable meeting time for both parties. Send a clarification email and confirm both accept.',
    B:'✗ Expecting a business partner to take a call at 11pm is unprofessional and a relationship risk. Catch this before it becomes a problem.',
    C:'✗ This is entirely solvable at your level — escalating a time zone confusion to the CEO creates unnecessary noise.'
  }}
];

let conflictsResolved = 0;
function resolveConflict(i, choice){
  const ans = CONFLICT_ANSWERS[i];
  const fb = document.getElementById('ccfb'+i);
  const card = document.getElementById('cc'+i);
  fb.textContent = ans.feedbacks[choice];
  fb.className = 'feedback-msg '+(choice===ans.correct?'feedback-ok':'feedback-warn');
  fb.style.display='block';
  if(choice === ans.correct && !card.classList.contains('resolved')){
    card.classList.add('resolved');
    card.querySelector('.conflict-label').innerHTML = '<i class="ti ti-check"></i> Conflict #'+(i+1)+' — Resolved ✓';
    conflictsResolved++;
    if(conflictsResolved>=3) document.getElementById('calBadge').classList.add('visible');
  }
}

// ============================================================
// SCENARIOS 4-8: PLACEHOLDER (show concept, unlock with pro)
// ============================================================
function buildStakeholderScenario(s,id){ return buildProPlaceholder(s,id,'✅','Track all 40 action items across 8 stakeholders, set due dates, flag overdue items, and generate a clean status report. Nothing can drop.'); }
function buildMeetingPrepScenario(s,id){ return buildProPlaceholder(s,id,'🗂','Prep all 5 back-to-back meetings: research each attendee, build agendas, write CEO brief notes, and package the complete prep kit.'); }
function buildApprovalScenario(s,id){ return buildProPlaceholder(s,id,'⚡','Review 4 vendor requests, decide your approval authority on each, process what you can, escalate CEO items with context, and log all decisions.'); }
function buildTravelScenario(s,id){ return buildProPlaceholder(s,id,'✈️','Book a 3-city trip leg by leg: flights, hotels, ground transport, schedule blocks, and build a complete print-ready itinerary for your CEO.'); }
function buildCRMScenario(s,id){ return buildProPlaceholder(s,id,'📊','Audit 20 stale contact records, update relationship notes, log last contact dates, flag dead leads, and produce a clean CRM report your CEO will actually trust.'); }

function buildProPlaceholder(s, id, icon, detail){
  return `
    <div class="scenario-brief">
      <div class="brief-label">📌 Scenario Overview</div>
      <div class="brief-text">${detail}</div>
    </div>
    <div style="text-align:center;padding:48px 24px;background:var(--navy3);border:1px solid var(--line);border-radius:12px">
      <div style="font-size:56px;margin-bottom:16px">${icon}</div>
      <div style="font-size:18px;font-weight:700;margin-bottom:8px">Unlock Full Scenario</div>
      <div style="font-size:13px;color:var(--muted);max-width:400px;margin:0 auto 24px;line-height:1.7">This scenario is part of <strong style="color:var(--gold)">EA Workflow Lab Pro</strong>. Unlock all 8 scenarios + your certified badge for a one-time payment of <strong style="color:var(--gold)">$29</strong>.</div>
      <div style="display:flex;gap:10px;justify-content:center;flex-wrap:wrap">
        <a href="https://paypal.me/jrvelado/29USD" target="_blank" class="btn-lg btn-gold" style="text-decoration:none"><i class="ti ti-brand-paypal"></i> Pay $29 via PayPal</a>
        <button onclick="activatePro()" style="background:transparent;border:1px solid #1D9E75;color:#1D9E75;padding:13px 24px;border-radius:10px;font-size:14px;font-weight:700;cursor:pointer;display:inline-flex;align-items:center;gap:8px">✅ Already paid — Activate now</button>
      </div>
      <div style="font-size:11px;color:var(--muted2);margin-top:14px">One-time · All 8 scenarios + certified badge · No subscription ever</div>
    </div>
  `;
}

function completeScenario(id){
  markComplete(id);
  closeModal();
  // Scroll to scenarios
  setTimeout(()=>{ document.getElementById('scenarios').scrollIntoView({behavior:'smooth'}); }, 200);
}

// ============================================================
// INIT
// ============================================================
document.addEventListener('DOMContentLoaded', function(){
  renderCards();
  updateOverallProgress();
  emailsActioned = 0;
  selectedTasks = new Set();
  conflictsResolved = 0;
});
</script>
</body>
</html>
